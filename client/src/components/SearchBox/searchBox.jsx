import React from ("react");
import "./searchBox.css";

function SearchBox({searchTerm, handleInputChange, handleSubmit}) {
     return (
          <form className="searchBox shadow-sm border border-success rounded">
               <div className="form-group">
                    
               </div>
          </form>
     )
}